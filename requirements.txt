# ss-to-wp-worker — Railway service dependencies
# Saskaņots ar sslv-ai-runner-railway/crm (kopīga koda atkarības)

# HTTP framework
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12  # File upload priekš /agent/image-upload (anketa-par-eku)

# Datu un API klienti (tāpat kā crm/)
psycopg[binary]==3.2.3
requests==2.32.3
python-dotenv==1.0.1

# PDF brošūru ģenerēšana (pdf_maker.py — HTML+CSS → PDF)
# Railway vajag system libs (pango utt.) — sk. Dockerfile
weasyprint==63.1

# Bilžu downscale PDF brošūrai (pdf_maker.py — 14 MB → ~2-3 MB)
Pillow==11.0.0

# PDF lapu apvienošana (bulk PDF endpoint — apvieno N listingus 1 brošūrā)
pypdf==5.1.0
